@*@{
    ViewBag.Title = "Home Page";
}

@(Html.Kendo().Grid<KendoMVCWrappers.Models.Person>().Name("people")
            .DataSource(dataSource => dataSource
                .Ajax()
                .Model(model => model.Id(m => m.PersonID))
                    .Read(read => read.Action("GetPeople", "Test"))
                    .Update(up => up.Action("UpdatePerson", "Test"))
            )

            .Columns(columns =>
            {
                columns.Bound(c => c.PersonID);
                columns.Bound(c => c.Name);
                columns.ForeignKey(c => c.ForeignKey, new SelectList(new[]{
                    new {text="t1",value = "1"},
                    new {text="t2",value = "2"},
                    new {text="t3",value = "3"},
                }, "value", "text"));
                columns.Command(cmd => cmd.Edit());
            })
            .Editable(ed => ed.Mode(GridEditMode.PopUp))
            .Pageable()
            .Sortable()
)*@
@(Html.Kendo().DatePicker().Name("test").Enable(false)

           .HtmlAttributes(new { style = "width: 650px; height:600px; display: none" })
)